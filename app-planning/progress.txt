================================================================================
  RESON8 — Development Progress Log
  Summary regenerated: 30/08/2026 (condensed from the original phase 1-8
  log; also condenses phases 9-10 as of 12/08/2026; now also condenses
  phases 11-13)
================================================================================

PROJECT OVERVIEW
================
Reson8 is a high-performance, self-hosted voice and text communication platform
for desktop (Electron). It replicates the TeamSpeak 3 experience — low-latency
voice, hierarchical channel trees, and private server ownership — while
integrating modern persistent text channels.

Tech Stack: Electron + TypeScript, Fastify + Socket.io, mediasoup (SFU),
PostgreSQL + Prisma, Redis, Docker.

Monorepo structure:
  apps/client/            — Electron desktop client
  apps/server/             — Node.js signaling + SFU server
  packages/shared-types/    — Shared DTOs, enums, and Socket.io event types

For build commands, environment variables, ports, and architecture details,
see the root CLAUDE.md (and apps/client/CLAUDE.md, apps/server/CLAUDE.md) —
not duplicated here to avoid drift between docs.

Full unabridged entry-by-entry history (every bug's root cause, every file
touched, every verification step) for everything summarized below lives in
app-planning/archive/progress-phases-1-8.txt (phases 1-8),
app-planning/archive/progress-phases-9-10.txt (phases 9-10), and
app-planning/archive/progress-phases-11-13.txt (phases 11-13). The original
PRDs that drove this work are in app-planning/archive/ (PRD.md,
Reson8_Evolutions_PRD.md, Reson8_Improvements_PRD.md,
Reson8_NextSteps_PRD.md, Reson8_Phase10_PRD.md, Reson8_Phase11_PRD.md,
Reson8_Phase12_PRD.md, Reson8_Phase13_PRD.md). Reach for the archive when
you need the "why" behind a specific decision that this summary
compresses away.


================================================================================
  PHASES 1-8 SUMMARY — ALL COMPLETE ✅  (current version: 1.1.0)
================================================================================

PHASE 1 — Signaling & Presence (Foundation)
  Fastify + Socket.io server (port 9800), Redis-backed presence tracking,
  Electron shell, typed Socket.io event maps, initial Prisma schema
  (Server/Channel/User/Role/Message), flat→nested channel-tree builder.

PHASE 2 — The Voice Bridge (mediasoup)
  mediasoup SFU integrated; full 6-step WebRTC handshake (GET_ROUTER_
  CAPABILITIES → CREATE_WEBRTC_TRANSPORT → CONNECT_TRANSPORT → PRODUCE →
  CONSUME → RESUME_CONSUMER); two-way audio; mute/deafen controls.
  Notable fixes: Electron mic-permission auto-grant, silent-audio race
  (queued consumption until recv transport ready), ICE announced-IP
  defaulting, detached-Audio()-element silence (must attach to DOM),
  deafen-toggle state flag, voice-control UI overflow.

PHASE 3 — Relational Logic & Hierarchy
  DB seeding (default server/channels/roles), channel CRUD over Socket.io,
  three-pane TeamSpeak-style UI (tree + log + status bar), click-to-join
  voice with occupant display, create/delete channel modals, production
  Docker Compose (server + Postgres + Redis).
  Notable fixes: window.confirm() unusable in Electron renderer (replaced
  with custom modal), modal input pointer-events/drag-region fix.

PHASE 4 — Permissions & Persistent Text
  Instance-based identity: persistent UUID per Electron install (fresh
  each launch in dev, persisted when packaged), auto-creates User +
  default role, ADMIN_INSTANCE_ID env var grants admin. Bitwise
  permission service + requirePermission() middleware. Permission-guarded
  channel CRUD. SEND_MESSAGE/FETCH_MESSAGES with cursor pagination.
  Tabbed chat UI. 11 unit tests added (permissions.test.ts).

PHASE 5 — Desktop UX, Audio Polish & Admin
  Unified Settings modal (Roles / Voice & Shortcuts tabs). Role management
  UI (toggle Member/Admin, admins can't self-demote). Audio device
  selection (input/output, localStorage-persisted). Multi-key shortcut
  combos for PTT/Mute/Deafen/Disconnect via Electron globalShortcut.
  URL-based server connection. Instance ID visible in footer.

PHASE 6 — Deployment & Packaging
  Server Dockerfile + entrypoint.sh (migrate → seed → start). Electron
  Builder config: Windows NSIS, Linux AppImage+deb, macOS dmg. Electron
  pinned to 34.5.8. Packaged-build instance ID persistence via
  app.isPackaged.

PHASE 7 — Evolutions (PRD 2.x — 14 items + moderation system)
  - Fixed dual-voice-channel race condition (full mediasoup + presence
    cleanup of the old channel before joining the new one; client-side
    join lock)
  - Public/private dual-announce IP (MEDIASOUP_PRIVATE_ANNOUNCED_IP) for
    hairpin-NAT self-hosters
  - Push-to-Talk Mode toggle, decoupled from the manual mute button
  - Explicit "Save" action for audio device changes (staged pending state)
  - Fixed channel-tree occupant hydration on initial connect
  - "Remember Me" — persists server URL/nickname/password in localStorage
  - Server password protection (SERVER_PRIVATE_PASSWORD env var)
  - Direct Messaging: full 1-to-1 DMs with persistence, Online Users
    modal, read receipts, unread indicators, auto-opening unread tabs
  - Rich media: image upload/paste sharing, dual storage (local disk or
    Cloudinary CDN), lightbox viewer
  - Active Speaker Indicator via server-side mediasoup AudioLevelObserver
    (pulsing halo on the channel-tree presence dot)
  - System Tray: minimize-to-tray, close-to-tray, tray context menu
  - Rich link previews (metascraper via main-process IPC, sanitized HTML,
    video embed support with fullscreen lightbox)
  - Emoji Selector: ~350 curated emojis, categorized + searchable
  - Native right-click context menu (Cut/Copy/Paste/Select All via
    Electron's Menu API)
  - Persistent DM conversations — history remains accessible once a
    partner goes offline (first-contact still requires both online)
  - Kick from Channel & Ban from Server moderation system (instance-ID-
    keyed bans with unban support)

PHASE 8 — Improvements (PRD 3.x — 5 items + 3 extras)
  - Fixed tray icon invisible in packaged builds (assets/**/* added to
    electron-builder's build.files)
  - Fixed "Unknown" nickname / duplicate-join-on-reconnect (resolveNickname()
    DB fallback for Redis-presence races; joinServerInFlight guard)
  - Sound Alerts System: 16 event-mapped audio cues (connection, voice
    presence, moderation, mute/deafen, DM notification gated by focus)
  - Mic Sensitivity Slider (noise gate): AnalyserNode-based silence
    detection with configurable dB threshold + live meter; 3 post-impl
    fixes (cloned-track analysis to avoid a mute feedback loop, threshold-
    only update to avoid re-hooking the analyser on every slider tick,
    preview mode for the meter outside of voice channels)
  - Voice Channel Session Timer: server-authoritative session start time,
    displayed in the channel tree and voice panel, synced on reconnect
  - Extra: Settings button always accessible even when disconnected
  - Extra: Emoji Reactions on messages (persistent, toggleable, tallied)
  - Extra: Real-time latency display (color-coded ping in the status bar)


================================================================================
  PHASES 9-10 SUMMARY — ALL COMPLETE ✅  (current version: 1.3.0)
================================================================================

PHASE 9 — Next Steps (PRD 4.x — 15 items, reached v1.2.0)
  - Per-user local voice volume (0–200%) and per-user local mute via a
    GainNode-per-participant Web Audio graph (client-local only, never
    sent to the server); localStorage-persisted per userId (PRD 4.1/4.2)
  - Self mute/deafen status icons: extended Redis presence + a new
    SET_VOICE_STATE event so isMuted/isDeafened (previously hardcoded
    false in 6 places server-side) reflect real client state (PRD 4.3)
  - Channel rename (reused an already-built but unwired UPDATE_CHANNEL
    event), NSFW text-channel flag + confirmation modal, and sibling
    drag-and-drop reordering via a new REORDER_CHANNELS event (atomic,
    MANAGE_CHANNELS-gated, admin-only drag affordance) (PRD 4.5/4.6/4.7)
  - Custom emoji uploads with an admin approval queue: new CustomEmoji
    model + MANAGE_EMOJIS permission, cover/pan/zoom crop tool, "+"
    picker tab, :name: token rendering shared with reactions (PRD 4.8)
  - 178 new default emoji (374 → 552 unique), plus a pre-existing
    duplicate (🔥) caught and fixed during the audit (PRD 4.9)
  - Delete own messages/DMs (hard delete) and edit own channel messages
    within a 2-minute server-enforced window; introduced
    storage.service.ts's deleteAttachment() (Cloudinary vs local-disk)
    shared by both delete paths and later reused by emoji rejection
    (PRD 4.10/4.11)
  - Instant upload feedback: local thumbnail + spinner appear the
    moment a file is picked/pasted, with an inline retry on failure
    (PRD 4.12)
  - Unread text-channel indicator via a new ChannelRead cursor table;
    tracked live client-side (MESSAGE_RECEIVED already broadcasts
    server-wide) with the cursor only needed to restore state on
    reconnect (PRD 4.13)
  - Shortcut-triggered mute/deafen/disconnect now play the same sound
    cues as their button equivalents (PRD 4.15)
  - Nudge: first true "server-wide setting" (Server.nudgeEnabled,
    admin-toggleable), 30s per-(sender,target) in-memory cooldown,
    sound + toast + taskbar/dock flash (PRD 4.14) — final item, all 15
    complete

  Post-Phase-9 fixes (found during manual verification, before Phase
  10 began):
  - Unread indicator reappearing after any voice join/leave (stale
    cached hasUnread on the tree node was never cleared by
    markChannelRead(), only the separate unreadChannelIds Set was)
  - Custom emoji upload silently doing nothing (CSP blocked `blob:` in
    img-src, so the crop tool's image preview never loaded/advanced)
  - Settings modal admin-tab flicker for non-admins (tab visibility now
    decided from a cached connect-time permission check, before the
    modal's first paint, instead of after an async re-check)

PHASE 10 — Auto-Updater, Audio Settings & Mute/Deafen Fixes (PRD 10.x —
6 items, reached v1.3.0)
  - Client auto-updater via electron-updater: check/download
    separation, 1-attempt-plus-3-retries-20s-apart shared by an
    automatic packaged-only startup check and a manual "Check for
    Updates" button, new "About" settings tab, GitHub Releases publish
    config (PRD 10.1)
  - Audio settings tab: Nudge / Sound Alerts / Global Voice Chat volume
    sliders (0–100%), the last multiplying into the existing per-user
    gain as a master attenuator (PRD 10.2)
  - Fixed the voice session timer blinking blank on every mute/deafen
    toggle (timer text now computed synchronously at render time
    instead of waiting for the next 1s interval tick) (PRD 10.3)
  - Mute/deafen accumulation: deafening now always auto-mutes the mic
    (remembering whether it had to), undeafening restores exactly the
    prior mute state, PTT is fully blocked while deafened, and clicking
    Mute while deafened auto-undeafens then applies a normal toggle —
    a user decision overriding the PRD's original "no-op" default
    (PRD 10.4)
  - Fixed a false-positive unread indicator on a user's own last
    message (excluded the sender's own messages from the unread query,
    and SEND_MESSAGE now advances the sender's own read cursor)
    (PRD 10.5)
  - Fixed the AppImage showing a generic icon instead of Reson8's on
    Wayland (explicit BrowserWindow icon, app.setName(), and a
    matching StartupWMClass) (PRD 10.6)


================================================================================
  PHASES 11-13 SUMMARY — ALL COMPLETE ✅  (current version: 2.2.0)
================================================================================

PHASE 11 — Client Fixes & Pinned Messages (PRD 11.x — 5 items, reached v1.4.0)
  - Fixed random voice disconnections: voice-channel auto-rejoin after a
    Socket.io reconnect, retuned ping/pong timeouts toward Socket.io's own
    defaults, ICE/connection-state monitoring on both transports with a
    grace-period restart attempt, mediasoup worker crashes no longer kill
    the whole server process (PRD 11.1)
  - Fixed the voice session timer briefly showing negative numbers right
    after joining, caused by a client/server clock-offset race (PRD 11.2)
  - Fixed the emoji picker's custom-emoji tab being invisible/hard to find
    (moved to its own fixed slot outside the scrollable category row)
    (PRD 11.3)
  - Post-update "What's New" modal: one-time, sourced live from GitHub
    release notes on first launch after an update (PRD 11.4)
  - Pinned messages: one pin per text channel, a bar above the chat with a
    preview that jumps to (and loads older history for, if needed) the
    pinned message (PRD 11.5)

PHASE 12 — Screen Sharing (PRD 12.x — 14 items, reached v2.0.0)
  - Screen sharing via mediasoup: full-screen or single-window capture,
    VP9 SVC (L1T3, after live testing proved K-SVC produces black video
    on real screen-content-flagged tracks — see progress-phases-9-10.txt
    -era pattern of trusting real capture over synthetic test tracks),
    a dedicated pop-out Viewer window, LIVE badges, server-wide toggle
  - Native per-app audio capture (Rust/NAPI-RS) for Windows and Linux,
    scoped to just the shared app rather than the whole desktop; not
    available on macOS
  - Custom stream names, sharer-awareness indicators (red banner, full-
    width Stop Sharing button, 🔴 in the window title), server name in
    the title bar
  - Admin-configurable message length limit, long-message truncation
    behind a "See more" button, client/server version-mismatch warning
  - A long tail of fixes surfaced during live multi-platform testing:
    the Wayland double-picker/redundant-modal issue, the VP9 black-video
    root cause above, an auto-updater failure investigation (v1.3.0 →
    v1.4.0 incident — missing latest*.yml release assets), a "What's
    New" modal skip bug for upgrades from very old versions, Windows
    audio-capture crashes, a Linux audio-share edge case, the emoji
    picker's custom-tab icon/scrollbar visibility, a stale "edit
    message" button, nudge-button width/blink, disabling Chromium's
    mic autoGainControl (Windows mic-gain drift), a Web-Audio-routing
    fix for per-user local volume/mute having no audible effect,
    "Unknown" nicknames after long sessions (a stale Redis presence
    hash with no refresh), deafen not applying to participants consumed
    after the toggle, and a broader connection-resilience pass for weak/
    flaky networks (persistent reconnection, a disconnect grace period
    so brief drops don't flicker presence, and more reliable Windows
    code-signing) — reached v2.1.0

PHASE 13 — Voice Quality, Chat/Emoji Polish & Admin Reorg (18 items from
app-planning/nextsteps.txt, reached v2.2.0)
  - AI noise cancelling: DeepFilterNet3 running via a vendored WASM build
    inside an AudioWorkletNode (not the npm package's default third-party
    CDN, to stay self-hosted); required an indirect-eval dynamic import
    to work around the dependency's ESM-only packaging, which otherwise
    crashed the entire preload script at startup — a real incident caught
    and fixed before release, now documented as a standing Electron
    gotcha in root CLAUDE.md
  - Noise gate rebuilt around a real attack/hold/release GainNode
    envelope instead of a hard `track.enabled` cutoff (was causing choppy
    audio); mic volume slider (0–200%); both extend one shared send-side
    Web Audio graph in `voice.service.ts` along with noise cancelling
  - Mic level meter detached from the noise gate toggle to be always
    visible in Voice & Shortcuts, reflecting any mic-related change live
  - NSFW channel images now blur by default with a reveal-on-click
    overlay; date-section dividers in text chat; animated custom emoji
    (GIF upload via a crop-free path); solo-emoji messages render ~4x
    size; screen-share sound alerts (start/stop sharing, viewer joined/
    exited your stream — the latter needed new server-side signaling)
  - Ban/Unban moved from the Online Users modal into a renamed "User
    Management" Settings tab (opens to either MANAGE_ROLES or BAN_USER
    holders, each control individually gated), now working for offline
    users via the existing GET_ALL_USERS listing
  - Single-instance Electron lock (focuses the existing window instead
    of spawning a redundant second instance)
  - "What's New" modal now renders markdown to HTML (via markdown-it, a
    real dual-CJS/ESM package this time) instead of showing GitHub's raw
    release-notes syntax verbatim
  - A batch of smaller UI/UX fixes: pointer cursor on voice participant
    names, higher-contrast participant-count badge, larger custom-emoji
    tab icon, a hang-up-phone icon for Leave Voice, a dedicated close
    button on the image lightbox, "See more" repositioned above the
    reaction bar and restyled as a pill, and a faster (300ms→100ms)
    active-speaker indicator interval
  - Two significant bugs found and fixed along the way: presence not
    clearing promptly after quitting the app (the client now explicitly
    disconnects before quitting so the server can finalize immediately,
    instead of always waiting out the reconnect-grace period meant for
    accidental drops), and the noise-cancelling preload crash mentioned
    above


================================================================================
  ONGOING LOG — new entries go below this line
================================================================================

Use `/log-progress` (see .claude/commands/log-progress.md) to append entries
here in the established format:

--- Entry: DD/MM/YYYY ---

Feature/Fix: <name>

  Problem: ...
  Solution: ...
  Key Files Modified:
    ...
  Verification:
    ...
  Next Step: ...

--- Entry: 30/08/2026 ---

Feature/Fix: Remove code signing from the client build pipeline; stop electron-updater rejecting unsigned updates on Windows

  Problem: The v2.2.0 GitHub release (built while a Windows Authenticode
  certificate was configured via CSC_LINK) shipped a signed installer. A
  user updating an already-installed, previously-signed client on Windows
  hit an auto-update failure: "New version 2.2.0 is not signed by the
  application owner" — the certificate chain terminated in an untrusted
  root ("Codegic CA G2"). Rather than chase a trusted certificate, the
  decision was to drop code signing entirely for now and ship unsigned
  installers until a better certificate is available.

  Solution: Removed the whole opt-in code-signing wrapper around
  electron-builder and disabled electron-updater's Windows signature
  check so unsigned updates are accepted:
  - Deleted `apps/client/scripts/build-signed.mjs` (the wrapper that
    loaded `.env.local` and shelled out to `electron-builder`) and
    `apps/client/scripts/local-env.mjs` (the `.env.local` loader it and
    `scripts/release-all.mjs` both used), plus the committed
    `apps/client/.env.local.example` template. `build:win`/`build:linux`/
    `build:mac` in `apps/client/package.json` now call `electron-builder`
    directly instead of going through the wrapper.
  - `scripts/release-all.mjs` no longer imports/calls `loadLocalEnv()`
    before its `electron-builder` steps.
  - Root-caused the actual runtime failure: `NsisUpdater` (from
    `electron-updater`) reads a `publisherName` baked into the
    *currently-installed* app's own `app-update.yml` (written by
    electron-builder at build time, from whatever signing cert was
    present then) and, if present, runs a PowerShell Authenticode check
    on every downloaded update against it — this is why a client that
    had been built+installed while still signed kept failing even once
    future builds stopped signing. Fixed in `apps/client/src/main.ts` by
    overriding `NsisUpdater`'s `verifyUpdateCodeSignature` (Windows only,
    behind a `process.platform === "win32"` guard) with a verifier that
    always resolves `null` — per electron-updater's own API this is a
    `(publisherNames, path) => Promise<string | null>` function, not a
    boolean, so `= false` alone does not compile and would not have
    worked correctly either.
  - Updated the "Code Signing" section in the root `README.md` to
    describe the current unsigned state instead of the old CSC_LINK
    setup guide (Windows SmartScreen / macOS Gatekeeper implications for
    an unsigned build, and a note to remove the `verifyUpdateCodeSignature`
    override again if signing is reintroduced later).
  - Added a new Electron gotcha bullet to root `CLAUDE.md` documenting
    this incident and the fix, so a future re-introduction of signing
    doesn't forget to remove the override.
  - Left `apps/client/certs/` and `apps/client/.env.local` (both
    gitignored, never tracked) untouched — they may contain the user's
    real private key/password material, so they were not read or
    deleted; the user can remove them by hand if desired.

  Key Files Modified:
    apps/client/src/main.ts
    apps/client/package.json
    apps/client/scripts/build-signed.mjs (DELETED)
    apps/client/scripts/local-env.mjs (DELETED)
    apps/client/.env.local.example (DELETED)
    scripts/release-all.mjs
    README.md
    CLAUDE.md

  Verification:
    npx tsc --noEmit in apps/client (confirmed the verifyUpdateCodeSignature
    override typechecks against electron-updater's actual NsisUpdater type,
    after first catching that a plain boolean assignment does not compile);
    node --check on scripts/release-all.mjs; a --dry-run of
    `node scripts/release-all.mjs` to confirm the loadLocalEnv removal
    didn't break its electron-builder invocation planning.

  Next Step: none — standalone fix. Next real code signing attempt (if
  any) should remove the verifyUpdateCodeSignature override in main.ts
  and restore a CSC_LINK-based signing flow once a certificate chaining
  to a trusted root is available.

