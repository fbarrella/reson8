================================================================================
  RESON8 — Development Progress Log
  Summary regenerated: 09/08/2026 (condensed from the original phase 1-8 log)
================================================================================

PROJECT OVERVIEW
================
Reson8 is a high-performance, self-hosted voice and text communication platform
for desktop (Electron). It replicates the TeamSpeak 3 experience — low-latency
voice, hierarchical channel trees, and private server ownership — while
integrating modern persistent text channels.

Tech Stack: Electron + TypeScript, Fastify + Socket.io, mediasoup (SFU),
PostgreSQL + Prisma, Redis, Docker.

Monorepo structure:
  apps/client/            — Electron desktop client
  apps/server/             — Node.js signaling + SFU server
  packages/shared-types/    — Shared DTOs, enums, and Socket.io event types

For build commands, environment variables, ports, and architecture details,
see the root CLAUDE.md (and apps/client/CLAUDE.md, apps/server/CLAUDE.md) —
not duplicated here to avoid drift between docs.

Full unabridged entry-by-entry history (every bug's root cause, every file
touched, every verification step) for everything summarized below lives in
app-planning/archive/progress-phases-1-8.txt. The original PRDs that drove
this work are in app-planning/archive/ (PRD.md, Reson8_Evolutions_PRD.md,
Reson8_Improvements_PRD.md). Reach for the archive when you need the "why"
behind a specific decision that this summary compresses away.


================================================================================
  PHASES 1-8 SUMMARY — ALL COMPLETE ✅  (current version: 1.1.0)
================================================================================

PHASE 1 — Signaling & Presence (Foundation)
  Fastify + Socket.io server (port 9800), Redis-backed presence tracking,
  Electron shell, typed Socket.io event maps, initial Prisma schema
  (Server/Channel/User/Role/Message), flat→nested channel-tree builder.

PHASE 2 — The Voice Bridge (mediasoup)
  mediasoup SFU integrated; full 6-step WebRTC handshake (GET_ROUTER_
  CAPABILITIES → CREATE_WEBRTC_TRANSPORT → CONNECT_TRANSPORT → PRODUCE →
  CONSUME → RESUME_CONSUMER); two-way audio; mute/deafen controls.
  Notable fixes: Electron mic-permission auto-grant, silent-audio race
  (queued consumption until recv transport ready), ICE announced-IP
  defaulting, detached-Audio()-element silence (must attach to DOM),
  deafen-toggle state flag, voice-control UI overflow.

PHASE 3 — Relational Logic & Hierarchy
  DB seeding (default server/channels/roles), channel CRUD over Socket.io,
  three-pane TeamSpeak-style UI (tree + log + status bar), click-to-join
  voice with occupant display, create/delete channel modals, production
  Docker Compose (server + Postgres + Redis).
  Notable fixes: window.confirm() unusable in Electron renderer (replaced
  with custom modal), modal input pointer-events/drag-region fix.

PHASE 4 — Permissions & Persistent Text
  Instance-based identity: persistent UUID per Electron install (fresh
  each launch in dev, persisted when packaged), auto-creates User +
  default role, ADMIN_INSTANCE_ID env var grants admin. Bitwise
  permission service + requirePermission() middleware. Permission-guarded
  channel CRUD. SEND_MESSAGE/FETCH_MESSAGES with cursor pagination.
  Tabbed chat UI. 11 unit tests added (permissions.test.ts).

PHASE 5 — Desktop UX, Audio Polish & Admin
  Unified Settings modal (Roles / Voice & Shortcuts tabs). Role management
  UI (toggle Member/Admin, admins can't self-demote). Audio device
  selection (input/output, localStorage-persisted). Multi-key shortcut
  combos for PTT/Mute/Deafen/Disconnect via Electron globalShortcut.
  URL-based server connection. Instance ID visible in footer.

PHASE 6 — Deployment & Packaging
  Server Dockerfile + entrypoint.sh (migrate → seed → start). Electron
  Builder config: Windows NSIS, Linux AppImage+deb, macOS dmg. Electron
  pinned to 34.5.8. Packaged-build instance ID persistence via
  app.isPackaged.

PHASE 7 — Evolutions (PRD 2.x — 14 items + moderation system)
  - Fixed dual-voice-channel race condition (full mediasoup + presence
    cleanup of the old channel before joining the new one; client-side
    join lock)
  - Public/private dual-announce IP (MEDIASOUP_PRIVATE_ANNOUNCED_IP) for
    hairpin-NAT self-hosters
  - Push-to-Talk Mode toggle, decoupled from the manual mute button
  - Explicit "Save" action for audio device changes (staged pending state)
  - Fixed channel-tree occupant hydration on initial connect
  - "Remember Me" — persists server URL/nickname/password in localStorage
  - Server password protection (SERVER_PRIVATE_PASSWORD env var)
  - Direct Messaging: full 1-to-1 DMs with persistence, Online Users
    modal, read receipts, unread indicators, auto-opening unread tabs
  - Rich media: image upload/paste sharing, dual storage (local disk or
    Cloudinary CDN), lightbox viewer
  - Active Speaker Indicator via server-side mediasoup AudioLevelObserver
    (pulsing halo on the channel-tree presence dot)
  - System Tray: minimize-to-tray, close-to-tray, tray context menu
  - Rich link previews (metascraper via main-process IPC, sanitized HTML,
    video embed support with fullscreen lightbox)
  - Emoji Selector: ~350 curated emojis, categorized + searchable
  - Native right-click context menu (Cut/Copy/Paste/Select All via
    Electron's Menu API)
  - Persistent DM conversations — history remains accessible once a
    partner goes offline (first-contact still requires both online)
  - Kick from Channel & Ban from Server moderation system (instance-ID-
    keyed bans with unban support)

PHASE 8 — Improvements (PRD 3.x — 5 items + 3 extras)
  - Fixed tray icon invisible in packaged builds (assets/**/* added to
    electron-builder's build.files)
  - Fixed "Unknown" nickname / duplicate-join-on-reconnect (resolveNickname()
    DB fallback for Redis-presence races; joinServerInFlight guard)
  - Sound Alerts System: 16 event-mapped audio cues (connection, voice
    presence, moderation, mute/deafen, DM notification gated by focus)
  - Mic Sensitivity Slider (noise gate): AnalyserNode-based silence
    detection with configurable dB threshold + live meter; 3 post-impl
    fixes (cloned-track analysis to avoid a mute feedback loop, threshold-
    only update to avoid re-hooking the analyser on every slider tick,
    preview mode for the meter outside of voice channels)
  - Voice Channel Session Timer: server-authoritative session start time,
    displayed in the channel tree and voice panel, synced on reconnect
  - Extra: Settings button always accessible even when disconnected
  - Extra: Emoji Reactions on messages (persistent, toggleable, tallied)
  - Extra: Real-time latency display (color-coded ping in the status bar)


================================================================================
  ONGOING LOG — new entries go below this line
================================================================================

Use `/log-progress` (see .claude/commands/log-progress.md) to append entries
here in the established format:

--- Entry: DD/MM/YYYY ---

Feature/Fix: <name>

  Problem: ...
  Solution: ...
  Key Files Modified:
    ...
  Verification:
    ...
  Next Step: ...

--- Entry: 11/08/2026 ---

Feature/Fix: Fix Shortcut Sound Alerts for Mute/Deafen/Disconnect (PRD 4.15)

  Problem: The mute/unmute and deafen/undeafen sound alerts only played
  when triggered via their on-screen buttons. Triggering the same
  actions via keyboard shortcuts produced no sound. Root cause: the
  click handlers (btnMute/btnDeafen/btnLeaveVoice) and the keyboard-
  shortcut keydown branches independently duplicated the same state-
  mutation logic (api.toggleMute()/toggleDeafen()/leaveVoiceChannel() +
  updateVoiceUI()), but only the click handlers ever called
  SoundAlert.play(...) — the shortcut path never had it added.

  Solution: Extracted the duplicated logic into three shared helpers —
  toggleMuteAndNotify(), toggleDeafenAndNotify(), leaveVoiceAndNotify()
  — each ending with the appropriate SoundAlert.play() call (preserving
  the existing PTT-mode lock/unlock branching inside the mute helper).
  Both the click handlers and the shortcut keydown branches now call
  these helpers instead of duplicating logic, so the two trigger paths
  can no longer drift apart. This also fixes the disconnect shortcut,
  which had the identical bug (no sound) despite not being explicitly
  named in the original report — same root cause, so fixed alongside
  mute/deafen per an explicit scoping decision made while drafting
  app-planning/Reson8_NextSteps_PRD.md (PRD 4.15).

  Key Files Modified:
    apps/client/src/renderer/renderer.ts — added toggleMuteAndNotify(),
      toggleDeafenAndNotify(), leaveVoiceAndNotify(); click handlers and
      shortcut keydown branches now call them instead of duplicating logic

  Verification:
    - TypeScript build (client): npx tsc --noEmit — passed

  Next Step: See PRD 4.3 entry below — its icon-toggle wiring hooks into
  the helpers added here.

--- Entry: 11/08/2026 ---

Feature/Fix: Session Timer Visibility Fix (PRD 4.4)

  Problem: The voice channel session timer (.session-timer) rendered in
  low-contrast gray (var(--text-muted), #6a6a7a) with an additional
  opacity:0.7 stacked on top, making it hard to read against the dark
  theme in both the channel tree and the voice panel.

  Solution: Switched the timer's color to var(--text-secondary)
  (#a0a0b0), the token already used elsewhere in the UI for this same
  "secondary but legible" contrast tier, and removed the opacity
  dampening. Pure CSS change — no timer logic touched.

  Key Files Modified:
    apps/client/src/renderer/index.html — .session-timer rule

  Verification:
    - Visual check: timer legible in both the channel-tree occupant row
      and the voice panel against the dark theme.

--- Entry: 11/08/2026 ---

Feature/Fix: Self-Mute / Self-Deafen Status Icons (PRD 4.3)

  Problem: When a user muted their mic or deafened themselves, other
  voice channel participants had no way to know — it looked like they
  were being ignored. IUserPresence.isMuted/isDeafened already existed
  in the shared type and in every PRESENCE_UPDATE payload, but were
  hardcoded to `false` in 6 separate places server-side (5 in
  connection.handler.ts, 1 in moderation.handler.ts), because
  voiceService.toggleMute()/toggleDeafen() were purely client-local and
  never told the server anything had happened — the fields existed but
  always lied.

  Solution:
    - Extended presence.service.ts's Redis-backed per-user hash to also
      store isMuted/isDeafened; added PresenceService.setVoiceState().
    - Refactored connection.handler.ts's duplicated resolveNickname()
      helper into an exported buildOccupant() that returns a full
      IUserPresence (including real isMuted/isDeafened) from a single
      presence.getUserPresence() call. Replaced all 6 hardcoded-false
      occupant-construction call sites (connection.handler.ts x5,
      moderation.handler.ts x1) with buildOccupant().
    - Added a new SET_VOICE_STATE client→server event
      (packages/shared-types/src/socket-events.ts), handled in a new
      handler in voice.handler.ts: updates presence, then rebuilds and
      re-broadcasts PRESENCE_UPDATE for the user's current channel
      (reusing connection.handler.ts's exported voiceSessionStartedAt
      map so this broadcast doesn't blank out the active session timer
      on other clients).
    - Client emits SET_VOICE_STATE from the toggleMuteAndNotify()/
      toggleDeafenAndNotify() helpers (added in PRD 4.15) and once right
      after successfully joining a voice channel, so state is synced
      correctly on join and after a reconnect.
    - renderOccupants() in renderer.ts now renders a small red
      (var(--danger)) inline-SVG mic-muted and/or headphones-deafened
      icon next to an occupant's name based on occ.isMuted/isDeafened.
      Also fixed updateOccupants(), which was silently dropping
      isMuted/isDeafened when applying live PRESENCE_UPDATE payloads to
      the tree (it previously only mapped userId/nickname through).

  Edge Cases Handled:
    - Deliberately did NOT wire this into the PTT press/release
      key-hold handlers. isMuted represents a deliberate mute action (or
      PTT-lock toggle), not momentary PTT silence between talk bursts —
      wiring it to every key press/release would cause the icon to
      flicker on every PTT tap with no real signal value. This was a
      considered deviation from the original plan file after re-reading
      the exact PTT code during implementation.
    - Reconnect-while-muted: state is re-synced to the server
      immediately after (re)joining a voice channel, so observers don't
      see a stale "unmuted" icon after someone drops and rejoins.

  Key Files Modified:
    apps/server/src/services/presence.service.ts — isMuted/isDeafened
      storage, setVoiceState()
    apps/server/src/handlers/connection.handler.ts — buildOccupant()
      (exported, replaces resolveNickname()), voiceSessionStartedAt
      exported, all 5 hardcoded occupant-construction sites replaced
    apps/server/src/handlers/moderation.handler.ts — uses buildOccupant()
    apps/server/src/handlers/voice.handler.ts — SET_VOICE_STATE handler
    packages/shared-types/src/socket-events.ts — SET_VOICE_STATE event
    apps/client/src/preload.ts — setVoiceState() bridge method
    apps/client/src/renderer/renderer.ts — SET_VOICE_STATE emission
      points, OCC_MUTED_ICON/OCC_DEAFENED_ICON, renderOccupants() and
      updateOccupants() updates, TreeNode.occupants type extended
    apps/client/src/renderer/index.html — .occ-voice-icon CSS

  Verification:
    - TypeScript build (shared-types): npx tsc --build — passed
    - TypeScript build (server): npx tsc --noEmit — passed
    - TypeScript build (client): npx tsc --noEmit — passed
    - Server test suite: npm run test — 19/19 passed, no regressions

  Next Step: PRD 4.1 (per-user local volume) and PRD 4.2 (local
  single-user mute) are next per app-planning/Reson8_NextSteps_PRD.md's
  suggested implementation order — they share the same GainNode-per-
  participant infrastructure and the generalized occupant right-click
  context menu.
